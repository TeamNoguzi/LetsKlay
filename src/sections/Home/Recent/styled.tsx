import styled from "@emotion/styled";

const RecentTitle = styled.h6`
  padding-left: 5px;
  margin-bottom: 25px;

  color: ${(props) => props.theme.colors.secondaryBold};
  font-weight: bold;
`;

export { RecentTitle };
