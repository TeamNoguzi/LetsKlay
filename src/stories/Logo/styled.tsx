import styled from "@emotion/styled";

const LogoText = styled.div`
  font-size: 32pt;
  color: ${(props) => props.theme.colors.primary};
  cursor: pointer;

  width: fit-content;
`;

export { LogoText };
