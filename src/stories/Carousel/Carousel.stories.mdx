import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Carousel from "./index";
import FundCard from "stories/Cards/FundCard";

<Meta title="Components/Carousel" component={Carousel} />

export const CarouselStory = (args) => {
  const project = {
    title: "test",
    subtitle: "testtest",
    summary: "hihihi",
    fundNow: 1000,
    fundGoal: 10000,
    thumbnailUrl: "public/Landscape-Color.jpg",
  };
  return (
    <Carousel {...args}>
      <FundCard project={project} width={270} />
      <FundCard project={project} width={270} />
      <FundCard project={project} width={270} />
      <FundCard project={project} width={270} />
      <FundCard project={project} width={270} />
      <FundCard project={project} width={270} />
      <FundCard project={project} width={270} />
      <FundCard project={project} width={270} />
      <FundCard project={project} width={270} />
    </Carousel>
  );
};

# Carousel

<Canvas>
  <Story
    name="default"
    args={{
      gap: 40,
      count: 9,
      division: 7,
      width: 1200,
      itemWidth: 270,
    }}
  >
    {CarouselStory.bind({})}
  </Story>
</Canvas>
