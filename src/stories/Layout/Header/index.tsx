import Logo from "stories/Logo";
import Navigation from "stories/Layout/Navigation";

const Header = () => {
  return (
    <header>
      <Logo center />
      <Navigation />
    </header>
  );
};

export default Header;
