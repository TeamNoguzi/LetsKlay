import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Stepper from "./index";

<Meta title="Components/Layout/Stepper" component={Stepper} />

export const StepperStory = (args) => <Stepper {...args} />;

# Stepper

<Canvas>
  <Story name="default" args={{ steps: ["hi", "by"] }}>
    {StepperStory.bind({})}
  </Story>
</Canvas>
