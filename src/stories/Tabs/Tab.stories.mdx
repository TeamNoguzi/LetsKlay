import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Tabs from "./index";

<Meta title="Components/Tabs" component={Tabs} />

export const TabsStory = (args) => <Tabs {...args} />;

# Tabs

<Canvas>
  <Story name="default" args={{ height: 60 }}>
    {TabsStory.bind({})}
  </Story>
</Canvas>
