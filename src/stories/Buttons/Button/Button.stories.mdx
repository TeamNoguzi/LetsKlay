import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Button from "./index";

<Meta title="Components/Buttons/Button" component={Button} />

export const ButtonStory = (args) => <Button {...args}>test</Button>;

# Button

## Button

<Canvas>
  <Story name="default" args={{ variant: "primary" }}>
    {ButtonStory.bind({})}
  </Story>
</Canvas>
