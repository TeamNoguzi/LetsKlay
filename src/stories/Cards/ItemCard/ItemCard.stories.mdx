import { Canvas, Meta, Story } from "@storybook/addon-docs";
import ItemCard from "./index";

<Meta title="Components/ItemCards" component={ItemCard} />

export const ItemCardStory = (args) => <ItemCard {...args} />;

# ItemCard

<Canvas>
  <Story name="default" args={{ bigSize: true, title: "default", body: "default", height: 300 }}>
    {ItemCardStory.bind({})}
  </Story>
</Canvas>
