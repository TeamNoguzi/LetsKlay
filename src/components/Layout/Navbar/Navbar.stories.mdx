import Navbar, {NavbarProps} from './Navbar';
import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta title="Layout/Navbar" component={Navbar}/>

export const Template = args => <Navbar {...args}/>;

## Navbar
네비게이션 바 컴포넌트
* 헤더 / 푸터
* pc / mobile
에 따라 디자인 변경함

### PC 헤더
<Canvas>
<Story
  name="Header"
  args={{
    type:'header',
    device:'pc'
  }}>
  {Template.bind({})}
</Story>
</Canvas>

### PC 푸터
<Canvas>
<Story
  name="Footer"
  args={{
    type:'footer',
    device:'pc'
  }}>
  {Template.bind({})}
</Story>
</Canvas>

### 모바일 헤더
* 모바일에서 검색 버튼을 누를 경우 검색창이 나오도록 함  

<Canvas>
<Story
  name="HeaderMobile"
  args={{
    type:'header',
    device:'mobile'
  }}>
  {Template.bind({})}
</Story>
</Canvas>

### 모바일 푸터
* 모바일에서 검색 버튼을 누를 경우 검색창이 나오도록 함  

<Canvas>
<Story
  name="FooterMobile"
  args={{
    type:'footer',
    device:'mobile'
  }}>
  {Template.bind({})}
</Story>
</Canvas>