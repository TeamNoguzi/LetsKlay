{
  "name": "letsklay",
  "workspaces": [
    "src",
    "server",
    "common"
  ],
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "yarn workspaces foreach -vip run dev",
    "compile": "yarn workspaces foreach -vip run compile",
    "migrate": "yarn worksapces foreach -vip run migrate",
    "build": "yarn workspaces foreach -vip run build",
    "start": "yarn workspaces foreach -vip run start",
    "lint": "yarn workspaces foreach -vip run lint"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/uuid": "^8.3.4",
    "@typescript-eslint/eslint-plugin": "^5.35.1",
    "@typescript-eslint/parser": "^5.35.1",
    "concurrently": "^7.0.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.2",
    "eslint": "^8.23.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-import-resolver-typescript": "^3.5.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.7.1",
    "typescript": "^4.4.2"
  },
  "packageManager": "yarn@3.2.3",
  "dependencies": {
    "add": "^2.0.6",
    "uuid": "^9.0.0",
    "yarn": "^1.22.19"
  }
}
