const enum ProjectStatus {
  "preparing",
  "funding",
  "ended",
  "cancelled",
}

const enum FundStatus {
  "valid",
  "cancelled",
  "refunded",
}

export { ProjectStatus, FundStatus };
